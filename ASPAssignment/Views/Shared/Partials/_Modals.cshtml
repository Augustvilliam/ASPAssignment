@* @model ASPAssignment.ViewModels.ProjectCreateForm

@{
    <div id="createprojectModal" class="project-modal modal fade" tabindex="-1" aria-labelledby="projectModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-body project-container">
                    <div class="form-header">
                        <h1 class="modal-title" id="projectModalLabel">Create Project</h1>
                        <button type="button" class="close-btn" data-bs-dismiss="modal" aria-label="Close">X</button>
                    </div>

                    <button type="button" class="upload-btn-team" id="projectUploadBtn">
                        <img id="projectPreviewImage" src="/img/upload.svg" alt="Project Image">
                    </button>
                    <input type="file" name="ProjectImage" id="projectImageInput" accept="image/*" style="display: none;" />

                    <form class="project-form" method="post" action="/Project/Create" enctype="multipart/form-data">

                        <label for="projectname">Project Name</label>
                        <input name="ProjectName" id="projectname" placeholder="Project Name" type="text">

                        <label for="clientname">Client Name</label>
                        <input name="ClientName" id="clientname" placeholder="Client Name" type="text">

                        <label for="desc">Description</label>
                        <textarea name="Description" id="desc" placeholder="Project Description"></textarea>

                        <div class="date-pick">
                            <label for="startdate">Start Date</label>
                            <input name="StartDate" id="startdate" type="date">

                            <label for="enddate">End Date</label>
                            <input name="EndDate" id="enddate" type="date">
                        </div>

                        <label for="members">Project Members</label>
                        <select name="SelectedMembersIds" id="members" multiple class="form-control"></select>

                        <label for="budget">Budget</label>
                        <input name="Budget" id="budget" type="text" placeholder="Budget i kr">

                        <button class="project-form-btn">Create</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <div id="createprojectModal" class="project-modal modal fade" tabindex="-1" aria-labelledby="projectModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-scrollable">
            <div class="modal-content">
                <div class="modal-body project-container">
                    <div class="form-header">
                        <h1 class="modal-title" id="projectModalLabel">Create Project</h1>
                        <button type="button" class="close-btn" data-bs-dismiss="modal" aria-label="Close">X</button>
                    </div>

                    <button type="button" class="upload-btn-team" id="projectUploadBtn">
                        <img id="projectPreviewImage" src="/img/upload.svg" alt="Project Image">
                    </button>
                    <input asp-for="ProjectImage" type="file" id="projectImageInput" accept="image/*" style="display: none;" />

                    <form class="project-form" method="post" asp-controller="Project" asp-action="Create" enctype="multipart/form-data" novalidate>

                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div class="text-danger mb-2">Please correct the errors in the form.</div>
                        }

                        <label asp-for="ProjectName"></label>
                        <input asp-for="ProjectName" class="form-input @(ViewData.ModelState["ProjectName"]?.Errors.Any() == true ? "input-validation-error" : "")" placeholder="Project Name" />
                        <span asp-validation-for="ProjectName" class="text-danger"></span>

                        <label asp-for="ClientName"></label>
                        <input asp-for="ClientName" class="form-input @(ViewData.ModelState["ClientName"]?.Errors.Any() == true ? "input-validation-error" : "")" placeholder="Client Name" />
                        <span asp-validation-for="ClientName" class="text-danger"></span>

                        <label asp-for="Description"></label>
                        <textarea asp-for="Description" class="form-input" placeholder="Project Description"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>

                        <div class="date-pick">
                            <label asp-for="StartDate"></label>
                            <input asp-for="StartDate" type="date" class="form-input @(ViewData.ModelState["StartDate"]?.Errors.Any() == true ? "input-validation-error" : "")" />
                            <span asp-validation-for="StartDate" class="text-danger"></span>

                            <label asp-for="EndDate"></label>
                            <input asp-for="EndDate" type="date" class="form-input" />
                            <span asp-validation-for="EndDate" class="text-danger"></span>
                        </div>

                        <label asp-for="SelectedMembersIds"></label>
                        <select asp-for="SelectedMembersIds" asp-items="ViewBag.Members" multiple class="form-control @(ViewData.ModelState["SelectedMembersIds"]?.Errors.Any() == true ? "input-validation-error" : "")"></select>
                        <span asp-validation-for="SelectedMembersIds" class="text-danger"></span>

                        <label asp-for="Budget"></label>
                        <input asp-for="Budget" class="form-input @(ViewData.ModelState["Budget"]?.Errors.Any() == true ? "input-validation-error" : "")" placeholder="Budget in kr" />
                        <span asp-validation-for="Budget" class="text-danger"></span>

                        <button type="submit" class="project-form-btn">Create</button>
                    </form>
                </div>
            </div>
        </div>
    </div>



    <div id="userModal" class="user-card profile-modal modal fade" tabindex="-1" aria-labelledby="userModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-body">
                    <button class="usersummary">
                        <img class="profile-thumb" src="@ViewBag.ProfileImage" alt="Profile" />
                        <p>@ViewBag.FullName</p>
                    </button>
                    <div class="darkmode-box">
                        <span class="dark-mode-lable">Dark mode</span>
                        <label for="darkmode-switch" class="switch">
                            <input type="checkbox" id="darkmode-switch" />
                            <span class="slider round"></span>
                        </label>
                    </div>
                    <button class="btn-custom btn-logout"><a asp-controller="Account" asp-action="Logout">Logout</a></button>
                </div>
            </div>
        </div>
    </div>


}
 *@